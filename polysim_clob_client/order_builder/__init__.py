"""py-clob-client-compatible ``order_builder`` subpackage.

The real package builds and EIP-712-signs orders here. In the paper SDK there
is no signing, so only the side constants (:mod:`.constants`) are mirrored.
"""
